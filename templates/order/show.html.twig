{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ order.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ order.lastname }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ order.adress }}</td>
            </tr>
            <tr>
                <th>Adress_complement</th>
                <td>{{ order.adressComplement }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ order.city }}</td>
            </tr>
            <tr>
                <th>Zip_code</th>
                <td>{{ order.zipCode }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ order.country }}</td>
            </tr>
            <tr>
                <th>Phone_number</th>
                <td>{{ order.phoneNumber }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ order.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_index') }}">back to list</a>

    <a href="{{ path('order_edit', {'id': order.id}) }}">edit</a>

    {{ include('order/_delete_form.html.twig') }}
{% endblock %}
